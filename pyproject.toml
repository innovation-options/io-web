[project]
name = "io-web"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs",
    "mkdocs-material",
]
[dependency-groups]
dev = [
    "ipython",
    "watchdog",
]
